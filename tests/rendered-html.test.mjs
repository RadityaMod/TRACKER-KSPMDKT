import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import test from "node:test";
test("static dashboard is a simple workbook-backed table",async()=>{const html=await readFile(new URL("../public/dashboard.html",import.meta.url),"utf8");const csv=await readFile(new URL("../public/data/laporan-ksp-mendekat.csv",import.meta.url),"utf8");assert.match(html,/Tracker Pelapor OCA/);assert.match(html,/Google Sheets/);assert.match(html,/<table>/);assert.match(html,/fetch\("\/data\/laporan-ksp-mendekat\.csv"\)/);assert.doesNotMatch(html,/sidebar|modal|timeline|dashboard-grid/);assert.equal(csv.trim().split(/\r?\n/).length,2);assert.match(csv,/No;Tanggal Masuk;Kanal;Nama Pelapor/);assert.match(csv,/Status Tindak Lanjut/);assert.doesNotMatch(csv,/;62\d{11};|;[a-z]{6};/)});